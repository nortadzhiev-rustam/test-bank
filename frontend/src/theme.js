import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#22D3EE",
      light: "#67E8F9",
      dark: "#0E9BB5",
      contrastText: "#06222B",
    },
  },
});

export default theme;
